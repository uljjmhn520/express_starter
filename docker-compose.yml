version: '2.1'
services:
  site:
    build:
      context: .
      dockerfile: Dockerfile
    image: "develop/node/custom_kakou"
    container_name: "dev_custom_kakou"
    ports:
      - "0.0.0.0:50006:50006"
    volumes:
      - app:/app
    env_file:
      - .env
    networks:
      - frontend
volumes:
  app:
    driver: local
    driver_opts:
      type: none
      device: $PWD
      o: bind
networks:
  frontend:
    external:
      name: frontend